@model UMS.Web.ViewModels.Departments.DepartmentGetAllViewModel
@{
    this.ViewData["Title"] = "All Departments, page " + Model.PageNumber;
}

@if (this.TempData.ContainsKey("Message"))
{
    <div class="alert alert-success">@this.TempData["Message"]</div>
}

<div>
    <h2 class="float-left">@this.ViewData["Title"]</h2>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary float-right">Back to Home page</a>
    <a asp-action="Create" class="btn btn-primary float-right" style="margin-right:1.2%">Create new department</a>
</div>

<br />

<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Id")
            </th>
            <th>
                @Html.DisplayName("Department name")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var department in Model.Departments)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => department.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => department.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = department.Id }, new { @class = "btn btn-success" })
                    @Html.ActionLink("Details", "ById", new { id = department.Id }, new { @class = "btn btn-info" })
                    @Html.ActionLink("Delete", "Delete", new { id = department.Id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </tbody>
</table>
<hr />
<partial name="_PagingPartial" model="@Model" />