@model UMS.Web.ViewModels.Students.StudentGetAllViewModel
@{
    this.ViewData["Title"] = "All Students, page " + Model.PageNumber;
}

@if (this.TempData.ContainsKey("Message"))
{
    <div class="alert alert-success">@this.TempData["Message"]</div>
}

<div>
    <h2 class="float-left">@this.ViewData["Title"]</h2>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary float-right">Back to Home page</a>
    <a asp-action="Create" class="btn btn-primary float-right" style="margin-right:1.2%">Create new student</a>
</div>

<br />

<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Id")
            </th>
            <th>
                @Html.DisplayName("Student name")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model.Students)
        {
            var fullName = student.FirstName + " " + student.LastName;
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => student.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => fullName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = student.Id }, new { @class = "btn btn-success" })
                    @Html.ActionLink("Details", "ById", new { id = student.Id }, new { @class = "btn btn-info" })
                    @Html.ActionLink("Delete", "Delete", new { id = student.Id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </tbody>
</table>

<hr />
<partial name="_PagingPartial" model="@Model" />